#pragma once
#include <DxLib.h>
// このファイルはExcelから自動生成されています。直接書き換えない

// 基本色
const unsigned int COLOR_RED = GetColor(255, 0, 0); // 赤
const unsigned int COLOR_GREEN = GetColor(0, 255, 0); // 緑
const unsigned int COLOR_BLUE = GetColor(0, 0, 255); // 青

// 補色
const unsigned int COLOR_MAGENTA = GetColor(255, 0, 255); // マゼンタ
const unsigned int COLOR_CYAN = GetColor(0, 255, 255); // シアン
const unsigned int COLOR_YELLOW = GetColor(255, 255, 0); // 黄

// 無彩色
const unsigned int COLOR_WHITE = GetColor(255, 255, 255); // 白
const unsigned int COLOR_BLACK = GetColor(0, 0, 0); // 黒
const unsigned int COLOR_GRAY = GetColor(128, 128, 128); // 灰
const unsigned int COLOR_LIGHT_GRAY = GetColor(200, 200, 200); // ライトグレー
const unsigned int COLOR_DARK_GRAY = GetColor(64, 64, 64); // ダークグレー
const unsigned int COLOR_SNOW = GetColor(255, 250, 250); // スノー
const unsigned int COLOR_GAINSBORO = GetColor(220, 220, 220); // ゲインズボロ
const unsigned int COLOR_DIM_GRAY = GetColor(105, 105, 105); // ディムグレー
const unsigned int COLOR_JET_BLACK = GetColor(20, 20, 20); // ジェットブラック

// 中間色
const unsigned int COLOR_ORANGE = GetColor(255, 165, 0); // オレンジ
const unsigned int COLOR_PURPLE = GetColor(128, 0, 128); // 紫
const unsigned int COLOR_BROWN = GetColor(139, 42, 42); // 茶
const unsigned int COLOR_AMETHYST = GetColor(153, 102, 204); // アメジスト
const unsigned int COLOR_AMBER = GetColor(255,191,0); // アンバー
const unsigned int COLOR_SAGE_GREEN = GetColor(140, 200, 120); // セージグリーン
const unsigned int COLOR_TEAL = GetColor(0, 128, 128); // ティール
const unsigned int COLOR_OLIVE = GetColor(128, 128, 0); // オリーブ
const unsigned int COLOR_FOREST_GREEN = GetColor(34, 139, 34); // フォレストグリーン
const unsigned int COLOR_INDIGO = GetColor(75, 0, 130); // インディゴ
const unsigned int COLOR_SLATE_BLUE = GetColor(106, 90, 205); // スレートブルー
const unsigned int COLOR_TAN = GetColor(210, 180, 140); // タン

// 明色
const unsigned int COLOR_SKYBLUE = GetColor(135, 206, 235); // スカイブルー
const unsigned int COLOR_PINK = GetColor(255, 182, 193); // ピンク
const unsigned int COLOR_LIME = GetColor(50, 255, 50); // ライム
const unsigned int COLOR_PALE_YELLOW = GetColor(255, 240, 150); // ペールイエロー
const unsigned int COLOR_MINT_GREEN = GetColor(120, 255, 200); // ミントグリーン
const unsigned int COLOR_CORNFLOWER_BLUE = GetColor(70, 150, 255); // コーンフラワーブルー
const unsigned int COLOR_SALMON_RED = GetColor(255, 128, 114); // サーモンレッド
const unsigned int COLOR_IVORY = GetColor(255, 255, 240); // アイボリー
const unsigned int COLOR_BEIGE = GetColor(245, 245, 220); // ベージュ
const unsigned int COLOR_PEACH = GetColor(255, 203, 164); // ピーチ
const unsigned int COLOR_BABY_BLUE  = GetColor(137, 207, 240); // ベビーブルー
const unsigned int COLOR_LAVENDER = GetColor(230, 230, 250); // ラベンダー
const unsigned int COLOR_MINT_CREAM = GetColor(245, 255, 250); // ミントクリーム

// 濃色
const unsigned int COLOR_NAVY = GetColor(0, 0, 128); // ネイビー
const unsigned int COLOR_MAROON = GetColor(128, 0, 0); // マルーン
const unsigned int COLOR_CHARCOAL = GetColor(54, 69, 79); // チャコール
const unsigned int COLOR_MIDNIGHT_BLUE = GetColor(25, 25, 112); // ミッドナイトブルー
const unsigned int COLOR_DARK_OLIVE_GREEN = GetColor(85, 107, 47); // ダークオリーブグリーン
const unsigned int COLOR_SADDLE_BROWN = GetColor(139, 69, 19); // サドルブラウン
const unsigned int COLOR_CRIMSON = GetColor(220, 20, 60); // クリムゾン
